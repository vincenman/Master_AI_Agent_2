[project]
name = "github-mcp-agent"
version = "0.1.0"
description = "GitHub MCP Agent"
dependencies = [
    "fastmcp",
    "mcp>=1.20.0",
    "PyGithub",
    "python-dotenv",
]

[tool.uv]
managed = true
